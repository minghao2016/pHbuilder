To-do
--------------------------------------------------------------------------------

-Add .mdp file / ph.dat file generation capability

Done
--------------------------------------------------------------------------------

-Change buffer.itp
-Add option to pythonlib.py to select wich version to load (atom A, atom B, etc.)
-Improve phbuilder.py structure/automation
    -Auto detect number of Asp/Glu and protonate (make neutral)
    -Figure out nicer way of adding to the topology
     (maybe make a class for generating topology?)
-Figure out energy group/coupling structure
-Add index file generator
